define(
    function (require) {

        /**
         * @class configs
         * @private
         * @static
         */

        var configs = {
            cache: true,
            dialogTitle : "BlendUI",
            dialogBtnOK : '确认',
            dialogBtnCancel : "取消",
            layerClass: 'layer',
            layerMainClass: 'layer-main',
            layersClass: 'layers'
        };

        return configs;

    }

);